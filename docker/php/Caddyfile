{
    auto_https off
    admin off
}

:8000 {
    root * /app/public
    encode zstd br gzip
    file_server

    php_server {
        try_files {path} index.php
        resolve_root_symlink
    }

    @phpFile {
        path *.php*
    }
    error @phpFile "Not found" 404
}
